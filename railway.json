{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun --bun run build && bunx prisma generate"
  },
  "deploy": {
    "startCommand": "bun --bun ./dist/server/entry.mjs",
    "sleepApplication": true
  }
}
