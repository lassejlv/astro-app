{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun --bun run build && bunx prisma generate && bunx prisma migrate deploy"
  },
  "deploy": {
    "startCommand": "bun --bun ./dist/server/entry.mjs",
    "sleepApplication": true
  }
}
